server:
  port: 8082
spring:
  config:
    activate:
      on-profile: prod
open:
  api:
    title: API Document
    version: v1.0.0
    description: Api Service
    serverUrl: http://localhost:${server.port}
    serverName: server test